#ifndef DISJOINT_SETS_H
#define DISJOINT_SETS_H

/**
 * 21.2 Linked-list representation of disjoint sets
 *      and with weighted-union heuristic.
 *  PS: 简单实现，只存取 0 ~ NumSets 的整数
 */

#define T ListSet

typedef int ElementType;

typedef struct T* T;

extern void makeset(T set, ElementType x);

extern ElementType findset(T set, ElementType x);

extern void unionset(T set, ElementType x, ElementType y);

#undef T
#endif
